/*
 * SUS WEARS type is self-hosted via src/styles/fonts.css (local woff2 subset
 * files in public/local-fonts). The CSS declares the
 *
 *   --font-sus-display  (Cormorant Garamond)
 *   --font-sus-sans    (Manrope)
 *
 * variables consumed by src/styles/tokens.css, so there is no runtime font
 * loader import needed here.
 */
export {};
